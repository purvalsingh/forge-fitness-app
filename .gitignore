node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea
coverage
.supabase

# CI workflow omitted: pushing it needs the GitHub "workflow" OAuth scope.
# To add it back: gh auth refresh -s workflow  (then restore .github/workflows/ci.yml)
